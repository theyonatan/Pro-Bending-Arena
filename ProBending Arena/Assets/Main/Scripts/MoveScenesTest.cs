using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using FishNet;
using FishNet.Managing.Scened;

public class MoveScenesTest : MonoBehaviour
{

    //private SceneManager _manager;
    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.K))
        //{
        //    _manager = InstanceFinder.SceneManager;

        //    //SceneLoadData sceneData = new SceneLoadData("Lobby")
        //    //{
        //    //    MovedNetworkObjects = new FishNet.Object.NetworkObject[1] {GameObject.Find("_NetworkLeader").GetComponent<FishNet.Object.NetworkObject>()}
        //    //};
        //    SceneLoadData sceneData = new SceneLoadData("Main")
        //    {
        //        ReplaceScenes = ReplaceOption.All,

        //        //MovedNetworkObjects = new FishNet.Object.NetworkObject[1] {gameObject.GetComponent<FishNet.Object.NetworkObject>()}
        //    };

        //    _manager.LoadGlobalScenes(sceneData);
        //}
    }
}
